---@type LazySpec
return {
  "gbprod/cutlass.nvim",
  opts = {
    cut_key = nil,
    override_del = true,
    exclude = {},
    registers = {
      select = "x",
      delete = "x",
      change = "x",
    },
  },
}
